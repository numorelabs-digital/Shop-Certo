import {and,eq} from "drizzle-orm";import {getDb} from "../../../../db";import {alerts} from "../../../../db/schema";import {ensureRequestUser} from "../../../../lib/request-user";export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){const user=await ensureRequestUser(request);if(!user)return Response.json({error:"Sesión requerida"},{status:401});const{id}=await params;await getDb().delete(alerts).where(and(eq(alerts.id,id),eq(alerts.userId,user.id)));return Response.json({deleted:true})}
