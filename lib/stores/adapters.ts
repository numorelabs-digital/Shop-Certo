import type {OfferResult,StoreAdapter} from "./types";
const pending=(url:URL,name:string):OfferResult=>({title:name,price:null,originalPrice:null,shipping:null,totalPrice:null,currency:"BRL",availability:"unknown",seller:null,image:null,lastUpdated:new Date().toISOString(),sourceUrl:url.toString(),status:"manual_required",error:"Integración automática pendiente; cargá el precio manualmente."});
class DomainAdapter implements StoreAdapter{constructor(public readonly id:string,private domains:string[]){} canHandle(url:URL){return this.domains.some(d=>url.hostname===d||url.hostname.endsWith(`.${d}`))}async fetch(url:URL){return pending(url,this.id)}}
export const storeAdapters:StoreAdapter[]=[new DomainAdapter("MercadoLivreAdapter",["mercadolivre.com.br"]),new DomainAdapter("AmazonAdapter",["amazon.com.br"]),new DomainAdapter("MagaluAdapter",["magazineluiza.com.br"]),new DomainAdapter("CarrefourAdapter",["carrefour.com.br"]),new DomainAdapter("ShopeeAdapter",["shopee.com.br"]),new DomainAdapter("GenericStoreAdapter",[])];
export function adapterFor(raw:string){const url=new URL(raw);if(!["https:"].includes(url.protocol))throw new Error("Solo se permiten URLs HTTPS");if(["localhost","127.0.0.1","::1"].includes(url.hostname)||url.hostname.endsWith(".local"))throw new Error("Dominio no permitido");return storeAdapters.find(a=>a.canHandle(url))??storeAdapters.at(-1)!}
